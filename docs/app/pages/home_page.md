# Home Page (`app/src/pages/HomePage.tsx`)

## 1. Overview
The entry page of the SPA. Contains:
- Hero banner introducing the AI interviewer.
- `CreateInterviewForm` for generating new sessions.
- `PreviousInterviewsList` displaying saved sessions from `localStorage`.
- Platform feature overview badges.
